loadtest {
  targetServer = "http://www.google.com"
  targetServer = ${?TARGET_SERVER}
  users = 1
  users = ${?USERS}
  duration {
    rampUpTime = 0
    rampUpTime = ${?RAMPUP_DURATION}
    time = 1
    time = ${?DURATION}
    unit = "minutes"
    unit = ${?DURATION_UNIT}
  }
}